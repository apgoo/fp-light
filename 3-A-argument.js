
// uninary function (shape)
function incremant(x) {
    return sum(x , 1)
}

// binary function (shape);
// x and y is parameter
function sum(x,y) {
    return x + y;
}

// it is an argument
incremant(5);


// function defenition variabel is an parameter
// that function call passes function paramete's value is argument
// here we see the argument (a input)

//  uninary - single parameter
//  binary  - two parameters
//  anary   - three to mutible(this shape of function is so hard to use)
// most of the time (dev) we use uninary and binary  




