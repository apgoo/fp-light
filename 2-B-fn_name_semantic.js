// name of the function is sematic
// change the function name to brobla
function f(x) {
    // f(x) = 2 x^2 + 3
    return (2 * Math.pow(x, 2)) + 3;
}


//sematic relation b/w input & outut;
// giving x value return the y value;

// if we draw a grph to in&output value we get a U saphe
//input and output is obiuse relationship

// x = 1 => y = 5
f(1);

// x = 2 => y = 11
f(2);

// x = 3 => y = 21
f(3);



/*
    # function naming is more impoertant for FP style
    # name is define us to relationship for both input and output
    # when we make a function obiaouse to set some relation for fn definition and call
    
*/ 